<div class="col-md-4">
    <form {{action "save" on="submit"}} >
        <dl>
            <dt>Name: {{ input value=model.name id='name' }}</dt>
        </dl>
        <div class="row">
            <button type="submit" class="btn btn-primary">Save</button>
        </div>
    </form>
</div>